services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./app:/app
      - node_modules:/app/node_modules
    ports:
      - "8080:8080"
    command: make dev

volumes:
  node_modules: